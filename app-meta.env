# Metadatos de la app — FUENTE ÚNICA para los scripts de build.
# Editar solo aquí. Lo leen build-macos.sh, build-windows.bat,
# los .spec de PyInstaller y el instalador de Inno Setup.
#
# Reglas: KEY=VALUE, sin comillas y sin espacios en los valores.
APP_NAME=Apipos
APP_VERSION=1.0.5
APP_PUBLISHER=Tecsom
APP_BUNDLE_ID=mx.tecsom.apipos
